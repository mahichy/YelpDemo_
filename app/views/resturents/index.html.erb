<p id="notice"><%= notice %></p>

<div class="jumbotron">
  <h2>Yelpdemo is the best platform to find reviews for local resturent</h2>
  <h3>Explore the fovorite diners, pastry shops, and entieries in your comunity. <br>
  Leave revious and ratings for your fellow foodies.</h3>
</div>

<table class="table table-hover table-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Website</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @resturents.each do |resturent| %>
      <tr>
        <td><%= resturent.name %></td>
        <td><%= resturent.address %></td>
        <td><%= resturent.phone %></td>
        <td><%= resturent.website %></td>
        <td><%= link_to 'Show', resturent, class: "btn btn-link" %></td>
        <td><%= link_to 'Edit', edit_resturent_path(resturent), class: "btn btn-link" %></td>
        <td><%= link_to 'Destroy', resturent, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Resturent', new_resturent_path, class: "btn btn-link" %>
