<%= form_with(model: resturent, local: true) do |form| %>
  <% if resturent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(resturent.errors.count, "error") %> prohibited this resturent from being saved:</h2>

      <ul>
      <% resturent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="field">
    <%= form.label :website %>
    <%= form.text_field :website %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
